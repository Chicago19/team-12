<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.codeforgood.poder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".UserLoginActivity"></activity>
        <activity android:name=".OrientationActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".ContactUsActivity" />
        <activity android:name=".AppointmentActivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".ExtraElementsActivity" />
        <activity android:name=".LearnMore3Activity" />
        <activity android:name=".LearnMore2Activity" />
        <activity android:name=".LearnMore1Activity" />
        <activity android:name=".ExamResults" />
        <activity android:name=".HomePageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DboardActivity" />
        <activity android:name=".PlacementExamActivity" />
        <activity android:name=".InterestedInWorkActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name" />
    </application>

</manifest>